import React from 'react';

const AuthContext = React.createContext(null);
const SearchContext = React.createContext(null);
const DropdownContext = React.createContext(null);
const DropdownwhatContext = React.createContext(null);
const MouseoutContext = React.createContext(null);

export {
    AuthContext, 
    SearchContext, 
    DropdownContext, 
    DropdownwhatContext,
    MouseoutContext,
};